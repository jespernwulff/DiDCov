% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sensitivity_intervals.R
\name{plot.sensitivity_intervals}
\alias{plot.sensitivity_intervals}
\title{Plot Method for Sensitivity Intervals}
\usage{
\method{plot}{sensitivity_intervals}(x, ...)
}
\arguments{
\item{x}{An object of class \code{sensitivity_intervals}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A ggplot object.
}
\description{
Creates a simple forest plot showing only the widest and narrowest intervals computed using
\code{compute_sensitivity_intervals} or \code{compute_sensitivity_intervals_smooth}.
}
\details{
If multiple values of \code{Mbar} are present in the results, this function will:
\itemize{
\item Identify the widest and narrowest intervals for each \code{Mbar} value.
\item Stack them vertically by using facets (one facet per \code{Mbar}), sharing the same x-axis scale.
}
}
\examples{
# Define simple data example
ub <- c(-0.002, 0.015)
lb <- c(-0.012, 0.0065)
beta <- c(-0.007, 0.011)
years <- 1:2

# Compute the sensitivity intervals
bounds_paper <- compute_sensitivity_intervals(
  betahat = beta,
  ci_lower = lb,
  ci_upper = ub,
  years = years,
  numPrePeriods = 1,
  numPostPeriods = 1
)

# Create a forest-style plot of the widest and narrowest intervals
plot(bounds_paper)
}
