% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_variances_from_ci.R
\name{compute_variances_from_ci}
\alias{compute_variances_from_ci}
\title{Compute Variances from Confidence Intervals and Effect Sizes}
\usage{
compute_variances_from_ci(
  years,
  effect_sizes,
  ci_lower,
  ci_upper,
  ci_level = 0.95
)
}
\arguments{
\item{years}{A numeric vector of years.}

\item{effect_sizes}{A numeric vector of effect sizes (estimates) corresponding to each year.}

\item{ci_lower}{A numeric vector of lower bounds of the confidence intervals corresponding to each year.}

\item{ci_upper}{A numeric vector of upper bounds of the confidence intervals corresponding to each year.}

\item{ci_level}{A numeric value between 0 and 1 indicating the confidence level of the confidence intervals. Default is \code{0.95}.}
}
\value{
A list containing:
\describe{
\item{\code{years}}{A numeric vector of years.}
\item{\code{variances}}{A numeric vector of variances computed from the confidence intervals.}
}
}
\description{
Computes the variances from given confidence intervals and effect sizes for each year in a diff-in-diff plot.
}
\examples{
# Example data
years <- c(2008, 2009, 2010)
effect_sizes <- c(0.1, 0.15, 0.2)
ci_lower <- c(0.05, 0.1, 0.15)
ci_upper <- c(0.15, 0.2, 0.25)

result <- compute_variances_from_ci(years, effect_sizes, ci_lower, ci_upper)
print(result$variances)

# Using the variances with the covariance matrix function
cov_matrix <- construct_cov_matrix(result$variances, rho = 0.5)
print(cov_matrix)
}
